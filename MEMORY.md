# MEMORY — BCD Survey Platform Session State

> Continuation/memory document for AI agents and future syncs.
> Update this file at the end of each working session and commit it so the
> context travels with the repo (local + GitHub).

## Goal

Build/continue the **BCD Generic Dynamic Survey Platform** at `C:\xampp\htdocs\bcd-app`, synced to GitHub (`https://github.com/amit200635k/bcd-app-mis`, branch `main`).

**FRESH INSTALL performed this session (fresh machine/htdocs copy, DB recreated from scratch):** backend fully set up and verified — `config/.env` + gitignored `config/app.php|database.php|jwt.php` created by hand (repo only ships `.env.example`; the config PHP files are REQUIRED and must be recreated on every fresh checkout), `composer install --no-dev` (see gotcha), `database/migrate.php --fresh` + all seeds run, smoke tests all green (after user-id fix), E2E govt-building section fixed for the new form id (40 → 2), API health/login/forms verified over Apache on :81. **Mobile:** npm install (`--legacy-peer-deps`), tsc/lint/jest clean, but `gradlew assembleDebug` FAILED on maven central DNS resolution (network) — APK NOT built yet. See "Mobile app runtime verification (this session)" in Milestones + new gotchas below.

**MOBILE2 (Capacitor, replaces the RN `mobile/` app) — this milestone:** the mobile app was re-built as **`mobile2/` — Capacitor 8 + TypeScript + Bootstrap 5 + Alpine.js + SQLite** (`com.jsac_bcd_survey.app`, minSdk 24 / targetSdk 36 / compileSdk 37, package id `bcd-mobile`). Web app via Vite (`npm run build` → `dist/`, `npm run sync` = build + `cap sync android`); Android Gradle builds need **JDK 21** (`C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot`) — JDK 17 breaks AGP 8.13. **Emulator = LDPlayer 9** (`D:\LDPlayer\LDPlayer9`, instance 0 = `emulator-5554`) — never the Android Studio AVD; NAT-only networking → `adb reverse tcp:8080 tcp:81` (ports <1024 can't be bound device-side) + `server.cleartext`/`CapacitorHttp` to dodge CORS; app hits `http://127.0.0.1:8080/bcd-app/api/v1` (dev; `src/js/config/index.ts` — release must use HTTPS). **Release APK built + verified signed this session:** `assembleRelease` succeeds (JDK 21, no network needed) → `mobile2/android/app/build/outputs/apk/release/app-release.apk` (~49.9 MB), signed with `android/app/bcd-survey-release.keystore` via `keystore.properties` (both gitignored — do NOT commit). `apksigner verify --print-certs` shows `CN=BCD Survey, O=JSAC, L=Ranchi, ST=Jharkhand, C=IN`. Debug APK: `assembleDebug` → `app-debug.apk`, install via `adb -s emulator-5554 install -r`, launch `am start -n com.jsac_bcd_survey.app/.MainActivity` (first `am start` after install may race teardown — retry). Run/build details in `mobile2/RUN_COMMANDS.md`. Full mobile runtime verification on LDPlayer still pending (see Next Steps).

Current state of the product: Phases 0–3 + parts of 5–8 of `ROADMAP.md`, plus an admin panel, a headed-Chrome E2E harness, master-data field type, dependent location-dropdown (cascade) field type, **conditional logic (IF/THEN show/hide/required)**, the "edit published form + sync to all users" flow, the full **Government Building Survey** form (17 sections / 132 fields, published), **RBAC portal + per-user form access** (admin assigns MIS/Admin portal + which surveys a user may fill/view), the expanded **mobile REST API** (records show/photos, devices, sync status), **server-side data-scope visibility** (every portal user sees only their own + sub-users' records/reports/GIS; admin sees all; record-detail page; full Masters tab), a **detailed, filterable records report** (`mis/detail_report.php`) with all-column filtering and scope-aware KPIs, **role-specific dashboards** (district / block / panchayat / village / surveyor each land on their own scoped home page after login; two new demo users cover the panchayat + village roles), and **server-side conditional-logic evaluation on record store** — hidden fields' answers are dropped before persisting and visible mandatory / condition-required fields are enforced (422 on the API), so stored records always reflect the form's rules; conditions also survive the edit + re-sync round-trip.

## Instructions / Environment

- PHP 8.2 via XAMPP, MariaDB 10.4.32 (root, no password, port 3306), Node 24, npm 11.5.1, Puppeteer 23.11.1.
- **`php` is NOT on PATH on this machine — always use the full path `D:\Xampp\php\php.exe`** (and for the E2E harness set `PHP_BIN=D:\Xampp\php\php.exe`, or its `reset.php` spawn fails with `ENOENT`). The CLI prints a harmless `openssl already loaded` warning; prefix `XDEBUG_MODE=off` for speed.
- **XAMPP Apache is on port 81** (port 80 is IIS on this machine). `APP_URL=http://localhost:81/bcd-app` is set in `config/.env` (gitignored).
- **Fresh install checklist (repo ships only `config/.env.example`):** (1) create `config/.env` (APP_URL port 81 + a real `JWT_SECRET`), (2) create the gitignored `config/app.php`, `config/database.php`, `config/jwt.php` (plain PHP arrays calling `env('APP_URL', …)` etc. — see files in `config/`; without them bootstrap dies with `Timezone ID '' is invalid` + `Unknown character set`), (3) `php composer.phar install --no-dev` (see gotcha re phpunit downloads), (4) `php database/migrate.php --fresh`, (5) seeds in order: `seed_demo.php` BEFORE `seed_jharkhand.php` is a FAIL (FK on districts) — run `seed_jharkhand.php` first, then `seed_demo.php`, `seed_govt_building.php`, `seed_portal_access.php`; (6) verify `curl http://localhost:81/bcd-app/api/v1/health`.
- Login: `admin` / `Admin@12345` (state admin). Demo users (`dh_surveyor`, `rk_surveyor`, `jb_block`, `sk_district`) have password `Demo@123`. Two more demo users cover the remaining hierarchy roles: `pm_panchayat` (panchayat role) and `vp_village` (village role), both `Demo@123`.
- `database/seed_govt_building.php` (already executed) created + published form `GOVT_BUILDING_SURVEY` (id=40, 17 sections / 132 fields) and master groups `DEPARTMENT` + `BUILDING_SUBCATEGORY`; `database/seed_portal_access.php` backfills portal access for admin + demo users.
  - ⚠️ **Ids are NOT stable across machines:** on this fresh DB `GOVT_BUILDING_SURVEY` is form **id 2** (old dev DB had 40), `DEPARTMENT` master group **id 2**, `BUILDING_SUBCATEGORY` **id 3** (old: 8/9), and demo users are `admin=1, dh_surveyor=3, rk_surveyor=4, jb_block=5, sk_district=6, pm_panchayat=7, vp_village=8` (id 2 is skipped by an auto-increment gap). Anything hardcoding those ids (E2E `run.js`, `tests/smoke.php`) must resolve by code/username instead.
- `.env`, `config/*.php` (except `.example.php`) are gitignored. `.gitignore` ignores `node_modules/` at any depth.
- Sync to git after every milestone (commit + `git push origin main`).
- Run smoke tests with `php tests/smoke.php`; E2E with `node tests/e2e/run.js` (headed Chrome; auto-resets DB at start/end).
- `tests/e2e/reset.php` purges E2E/smoke artifacts (`E2E_`, `e2e_`, `test.`, `E2E-`, `SMOKE_`, `Smoke`, `Test Dist`, `E2E_GRP_%`) so the demo stays pristine.
- Follow the phasing in `ROADMAP.md`.

## Key Architecture

- **DB schema**: `database/schema.sql`; seed data via `database/seed_demo.php` + `database/seed_jharkhand.php` (idempotent, 24 districts / 96 blocks / 288 panchayats / 1152 villages).
- **Core services** in `common/src/Services/` (SurveyService, RecordService, UserService, LocationService, NotificationService, ReplicationService, ReportService).
- **Auth**: session (`SessionAuth`, MIS + admin) and JWT (`ApiAuth`, mobile REST via `api/index.php`).
- **Router**: `common/src/Http/Router.php` (route params `{id}`).
- **API front controller** `api/index.php`; `.htaccess` rewrites `^api(?:/(.*))?$` only when the physical file doesn't exist.
- **MIS pages** under `mis/`, admin panel under `admin/`, layouts in `common/views/`.

## Discoveries & Gotchas (IMPORTANT)

- Apache port 80 is IIS → test via `http://localhost:81/bcd-app`.
- **Authorization header bug (FIXED):** XAMPP Apache exposes `Authorization` via `getallheaders()` but NOT `$_SERVER['HTTP_AUTHORIZATION']`. `Request::header()` now falls back to `getallheaders()`; without this, every token-authed API call returns 401.
- `.htaccess` only rewrites `^api` to the front controller when a physical file doesn't exist, so `/api/dropdowns.php` and `/api/user_data.php` remain real scripts.
- MIS sidebar uses `url()` absolute links; admin sidebar uses relative links (admin pages live in `admin/`).
- `smoke.php` pollutes the live DB (creates `Test Dist`, `SMOKE_*` forms, `Smoke` notification) — `reset.php` cleans those too.
- `SurveyService::saveStructure()` accepts both `label/value` and `option_label/option_value` option conventions and skips empty option rows.
- `mis/users/index.php` `openModal()` must reset fields **after** async dropdown loading (race fixed).
- **`UserService::create()`/`update()` must convert empty-string fields (`''`) to `null`** for FK columns (district_id, block_id, etc.) — empty strings violate FK constraints.
- **`UserService::setPortals()`/`setFormAccess()` must NOT open their own transactions** — they are called inside `create()`/`update()` outer transactions (nested transactions throw "already an active transaction").
- **Bootstrap modals opened from JS (`openModal`, `openAccess`) must call `bootstrap.Modal.getOrCreateInstance(...).show()` and should show the modal FIRST, then populate asynchronously** — showing after a fetch leaves the modal invisible during the request (flaky).
- **E2E typing during a Bootstrap modal fade / in-flight `loadLocations()` fetch truncates typed text** in headed Chrome (e.g. `e2e_64813915` → `e2e_6481`). The `type()` helper in `tests/e2e/lib.js` now verifies the field value and retries up to 3×; tests also `waitForNetworkIdle` after opening a modal.
- **Location cascade change-handler bug (FIXED):** `mis/builder/preview.php` re-populated the *current* select on change instead of the *next* one — selecting a district never populated blocks. Fix: a `populate(target, level, parentValue, selectedId)` helper driven from the change handler with the *next* select/level.
- `plain_password` column on `users` is dev-only (null in production), shown as “Password (dev)” in MIS users list, stripped from `user_data.php`.
- `seed_jharkhand.php` is idempotent (`ON DUPLICATE KEY UPDATE`); child rows re-query parent ids (never rely on `lastInsertId` with upserts).
- **`hasText()` in E2E uses `document.body.innerText`, which EXCLUDES `display:none` elements** — a field hidden by a conditional engine (`.d-none`) will NOT match `hasText`. Test hidden fields with `page.evaluate(querySelector)` on the DOM instead of `hasText`.
- **Android emulator runtime (this machine, from mobile runtime-testing):** AVD `Medium_Phone_API_36` (x86_64, API 36, PlayStore). Headless boot that works: `"$LOCALAPPDATA/Android/Sdk/emulator/emulator.exe" -avd Medium_Phone_API_36 -no-snapshot -no-audio -no-boot-anim -gpu swiftshader_indirect -no-window` (WHPX hypervisor present; `-no-window` + software GPU is stable). `sys.boot_completed=1` takes ~5 polls at 10s. The `emulator` binary is NOT on PATH — use the full path under `%LOCALAPPDATA%\Android\Sdk\emulator\`. **This machine's only AVD is `Pixel_4a`** — use `-avd Pixel_4a` with the same flags.
- **CRITICAL — do NOT run `adb kill-server` while the emulator runs:** the emulator drops to `offline` permanently and won't recover via `adb reconnect` — the only fix is `taskkill //F //IM qemu-system-x86_64-headless.exe` + relaunch the emulator. Restart adb only as a last resort.
- **`php` is NOT on PATH** → use `D:\Xampp\php\php.exe` everywhere; E2E `run.js` now honors `PHP_BIN` env (else `reset.php`/fixture spawns fail with `ENOENT`). CLI always prints a benign `Module "openssl" is already loaded` warning.
- **mobile2 release signing (IMPORTANT):** `android/keystore.properties` (storeFile=bcd-survey-release.keystore, alias bcd-survey) + `android/app/bcd-survey-release.keystore` are gitignored — never commit them; after a fresh clone recreate `keystore.properties` before `assembleRelease`. `app/build.gradle` reads it only when the file exists.
- **Smoke "Detail range filter" is data-dependent:** the check computes the expected count from the data — number of all records whose `built_up_area` is numeric within 0..1,000,000 (a record without a `built_up_area` answer is correctly excluded, so it no longer asserts `count === all`; was failing with all=1 range=0 when the only form-2 record lacked `built_up_area`).
- **`composer install` (with dev deps) fails on this machine:** `sebastian/*` (PHPUnit) dist zips from GitHub fail with `Permission denied` writing `vendor/composer/tmp-*.zip` (plain file writes work; manual download of the same zip succeeds — AV/curl interference). **Workaround: `composer install --no-dev`** — the runtime has ZERO external deps (only `php` + `ext-*`), and PHPUnit isn't used by the smoke/E2E workflows. `--prefer-source` hangs >10 min; skip it.
- **`database/migrate.php` `splitStatements()` did not skip SQL comments** — semicolons inside `--` comments (`schema.sql:176`, `seed.sql:61`) produced corrupt statements (`Syntax error near 'state admin is implicit all'`). Fixed: skip `--`, `#`, `//` line comments + `/* */` blocks. Keep comments semicolon-free OR keep the fixed runner.
- **Migration 003 duplicates seed.sql grants** (`Duplicate entry '4-14' for key 'PRIMARY'` on fresh install) → now `INSERT IGNORE`. Migrations must be idempotent against seed.sql on fresh installs.
- **`tests/smoke.php` hardcoded demo-user ids** (`User::find(3|4|5)`) that matched the OLD dev DB → fails `Surveyor scope is own id only` on fresh installs (dh_surveyor id 3 has role `district`). Fixed via `User::findByUsername('rk_surveyor'|'jb_block'|'sk_district')`. Don't reintroduce bare ids; user ids are NOT stable across machines (see Goal).
- **E2E `run.js` hardcoded the govt form id 40 + master ids 8/9** (old dev DB) → `Waiting failed: 15000ms exceeded` opening `builder/edit.php?id=40`. Fixed: `const govt = { form: 2, dept: 2, sub: 3 };` at the top of `run.js` (fresh-DB ids; see Goal for the mapping). The `#ac_portal_mis` admin-modal wait also flaked once in the full run — a manual probe reproduced the modal visible fine (retry-able).
- **Root `/bcd-app/` returned 403** (`Options -Indexes`, no index file) → new root `index.php` redirects to `mis/login.php`. Any page under `/` needs a real file.
- **AVD on this machine is `Pixel_4a`** (x86_64, API 36, `PlayStore.enabled=false`); `Medium_Phone_API_36` does NOT exist here. Boot with the same headless flags, substituting the AVD name.
- **`npm install` in `mobile/` fails with ERESOLVE** on `@react-native-async-storage/async-storage@2.1.2` vs RN `0.87.0-rc.3` (prerelease peer ranges) → **must use `npm install --legacy-peer-deps`**. Same flag likely needed for any future dep add while on the rc line.
- **`gradlew assembleDebug` failed on `repo.maven.apache.org` hostname resolution** (DNS) after ~14 min — network, not code; retry when the connection is stable. Build produces `mobile/android/app/build/outputs/apk/debug/app-debug.apk`.
- **Metro on Windows must be started fully detached** (bash `&` backgrounds die when the tool call exits → `ERR_STREAM_WRITE_AFTER_END`). Working method: `powershell -Command "Start-Process -FilePath 'cmd.exe' -ArgumentList '/c','npx react-native start --port 8081 > %TEMP%\metro3.log 2>&1' -WorkingDirectory 'C:\xampp\htdocs\bcd-app\mobile' -WindowStyle Hidden"`. Only ONE instance may bind 8081 (a second → `EADDRINUSE`). Verify with `curl http://localhost:8081/status`.
- **RN debug APK needs Metro:** the debug variant does NOT embed `index.android.bundle`, so first launch logs `Unable to load script` + `BridgelessReact`/`ReactHost` errors until Metro is reachable. Enable reachability with `adb reverse tcp:8081 tcp:8081`, relaunch (`am force-stop com.bcdmobile` → `am start -n com.bcdmobile/.MainActivity`), and watch Metro's log for the bundle build. `app-debug.apk` installs via `adb install -r`; package/activity = `com.bcdmobile/.MainActivity`.

## Feature Blueprints (current design decisions)

### Master-data field type (`master`)
- Admin CRUD at `admin/masters.php` (groups + items; system groups like `DISTRICT` are undeletable; audit-logged).
- Field settings: `settings_json → {"master_group_id": N}`.
- `SurveyService::fields()` hydrates master fields with `options` = active `master_items` (label=name, value=id) + `master_group_id/name`.
- `RecordService::normalizeMaster()` stores answer as `value_text = item name`, `value_json = {"master_id":N,"name":"…"}` (resolves by id, falls back name→id by group).
- `master_items` has `UNIQUE KEY uq_master_items_group_code (group_id, code)` for idempotent sync.
- DISTRICT master items (24 districts) are synced from the `districts` table by `seed_jharkhand.php`.

### Dependent location dropdowns (`location_cascade`)
- Field type chains District → Block → Panchayat → Village; builder checkboxes pick included levels → `settings.levels`.
- API (Bearer auth, for mobile):
  - `GET /api/v1/location/children?level=block&parent_id=5` → `{scope, items:[{id,name}]}`.
  - `GET /api/v1/location/scope` → user's fixed scope `{district_id, block_id, panchayat_id, village_id}`.
- Session-based feed for MIS preview: `api/dropdowns.php?type=scope|district|block|panchayat|village`.
- Scope-aware: state admin sees all districts and cascades down; district/block-scoped user gets the topmost scoped level pre-selected + locked and the next level auto-populated.
- `RecordService::normalizeLocation()` stores `value_text = "Ranchi / Kanke / …"` and `value_json = {"district_id":…,"district_name":…,…}`.
- Mobile client (`mobile/`) calls `/v1/location/scope` on form load and `/v1/location/children` on each select change.

### Conditional logic (IF/THEN show/hide/required)
- `survey_conditions` table: `field_id` = the **controlled** field (FK), `target_field_id` = the **trigger** field (FK), `operator` ENUM(`equals`,`not_equals`,`in`,`not_in`,`greater_than`,`less_than`,`contains`), `condition_value`, `action` ENUM(`show`,`hide`,`required`), `sort_order`. A condition row lives on the *controlled* field and references the *trigger* field.
- **Builder UI** (`mis/builder/edit.php`): each field gets a "Conditions — IF target = value THEN action" editor. Editor state uses `conditions: [{target_field_key, operator, condition_value, action}]` (key-based, NOT id-based). `allFields()` builds the target dropdown; operators/actions from `COND_OPERATORS`/`COND_ACTIONS`.
- **Save** (`SurveyService::saveStructure()`): two-pass — sections/fields inserted first building a `fieldIds[field_key]` map, then conditions inserted last (supports **forward references** where a condition targets a field defined later). `target_field_id` is taken verbatim if provided, else resolved from `target_field_key`.
- **Load** (`SurveyService::sections()`): each condition gets `target_field_key` resolved from `target_field_id` (via field id→key map) so UI/API consumers match against answers.
- **Preview engine** (`mis/builder/preview.php`): conditions embedded as JSON keyed by controlled `field_key`; `matches()` implements all 7 operators; `applyConditions()` toggles `.d-none` + `required` (and a `.cond-star` for condition-required). Listens on `input` + `change`. Runs on page load, so a hidden field is `display:none` from the start.
- **Server-side evaluation on record store (Phase 3, done):** `ConditionEvaluator` (NEW) mirrors the preview engine — `evaluate(sections, answers)` → `{visible, required}` maps; `missingRequired()` returns `field_key → [message]` for visible fields that are mandatory OR condition-required but empty. `RecordService::upsert()` runs it against the submitted `form_version_id`'s definition: **hidden-field answers are dropped before persisting** and (for non-`draft` statuses) a `ValidationException` (NEW, `App\Exceptions`) is thrown when a visible mandatory/condition-required value is missing; `RecordController::store()` maps it to **422** with the field errors. Drafts skip required-validation but still drop hidden answers. Conditions were already exposed to mobile via `/v1/forms/{id}` (each condition carries its resolved `target_field_key`).
- **Round-trip bug fixed:** `SurveyService::saveStructure()` now resolves each condition's target by `target_field_key` against the freshly inserted field-id map (preferred over a stale `target_field_id` that references the previous version's deleted fields), so conditions survive the edit-published-form → Save & Sync flow. It also accepts `is_mandatory` (as well as `mandatory`) so mandatory survives builder state round-trips (`sections()` returns `is_mandatory`, not `mandatory`).

### Edit published form + sync to all (web + mobile)
- **Flow:** published form → admin clicks edit → `SurveyService::draftForEditing()` resumes the existing non-empty draft OR clones the latest published structure into a new draft (so the editor opens a working copy of the live form, never a blank page). Admin edits the draft, then clicks **"Save & Sync to All"**.
- `draftForEditing(int $formId, int $userId)` — resumes latest draft only if it has structure (`hasStructure()` = ≥1 section); otherwise creates a new draft cloned from the latest published version. Works for both published and never-published forms.
- `versionInfo(int $formId)` → `['published_version' => int, 'draft_version' => int, 'pending_changes' => bool]` (pending = draft_version > published_version).
- `mis/builder/sync.php` (NEW): requires `survey_builder.publish`; guards "no pending draft changes"; calls `publish()` then `NotificationService::send(title, body, null, null, $user->id(), 'info')` to **broadcast to all active users** (web + mobile get a "new form version" notification); writes `survey.sync` audit entry; flashes "Draft published as vN and synced to all users (web + mobile).".
- `mis/builder/edit.php`: POST `action=save` (save draft) or `action=save_sync` (save then redirect to `sync.php`). Header shows `published vN` badge + `pending changes — vN` badge; **Save & Sync to All** button only for users with `survey_builder.publish`.
- `mis/builder/index.php`: version column shows `vN draft` pending badge when `pending_changes`; action buttons — sync button (`sync.php`) for published+pending (publish permission), else rocket publish button (`publish.php`) for unpublished forms. Edit (pencil) always available.
- Publish permission gate: `survey_builder.publish`. Non-publishers can edit/save drafts but see no sync/publish buttons.
- Gotcha: `createForm()` always creates an empty draft v1; `draftForEditing()` ignores it (no structure) and clones from published instead.

### RBAC portal + per-user form access
- Tables: `user_portal_access(portal ENUM('mis','admin'))`, `user_form_access(form_id, can_fill, can_view)` — migrations in `database/migrations/002_portal_form_access.sql`.
- `User` helpers: `portals()`, `hasPortal($p)` (state_admin implicit all), `assignedFormIds()`, `canAccessForm($id)` (state_admin implicit all).
- `UserService` is **scope-aware**: `list/create/update` enforce the acting user's hierarchy (`scopeConditions()`, `assertInScope()`); `assignableRoles()` limits which roles may be granted by each role; `assignableForms()` filters to published forms the actor may grant; `setPortals()/setFormAccess()/updateAccess()` assign access (no nested transactions).
- `mis/users/index.php` — create/edit modal has portal checkboxes (Admin only if `canGrantAdmin`) + form multiselect; roles/forms are scoped to the actor.
- `admin/access.php` (state-admin only) — Roles & Access page: list users, manage portals + forms per user, audit-logged.
- `mis/login.php` — after auth, gates on `hasPortal('mis')`; admin panel pages are state-admin only.
- API exposure: `api/user_data.php` returns `portals` + `form_ids`; `FormController::index()`/`show()` filter by `canAccessForm()` (403 otherwise); `RecordController::store()` checks access too.
- `database/seed.sql` grants `dashboard.view`, `users.view`, `users.manage` to district/block/panchayat/village roles (applied to live DB).

### Role-specific dashboards (this milestone)
- **Routing:** `User::homeUrl()` returns the landing page for the user's role (`mis/dashboard.php` for state_admin; `mis/home_district.php` / `home_block.php` / `home_panchayat.php` / `home_village.php` / `home_surveyor.php` for the hierarchy + surveyor roles). `mis/login.php` redirects to `homeUrl()`; the sidebar "Dashboard" link uses it too; `mis/dashboard.php` redirects non-state-admins to their role page (it is admin-only and no longer leaks unscoped counts).
- **`RoleDashboardService` (NEW)** — `roleOf(User)` returns the highest-priority role (state_admin → district → block → panchayat → village → surveyor → department_admin) and `stats(User)` returns scoped aggregates: `unit` (name of the viewer's lowest admin unit), `unit_type`, `children` (sub-unit counts: district→blocks/panchayats/villages, block→panchayats/villages, panchayat→villages), `users{total,surveyors}` (active users in the unit), `records{total,by_status,per_form,latest}` (scoped via `RecordService::scopeUserIds`), `forms` (count of accessible published forms). Surveyors get no user/unit aggregation — only their own submissions.
- **Pages** `mis/home_*.php` are thin wrappers that guard the role (`roleOf($user) !== X` → redirect to `homeUrl()`) and render `common/views/partials/role_dashboard.php`, which shows unit banner, role-appropriate stat cards (Records in scope / My Submissions, Users, Surveyors, sub-units, Surveys you can access, + submitted/approved/rejected for surveyors), Records by Status, Top Surveys by Records, Latest Submissions (with `records.php?id=` links), and Quick Actions (Monitoring / Reports / Detailed Report / Manage Users, gated by permission).
- **New demo users:** `pm_panchayat` (panchayat, Ranchi→block 77→panchayat 229) and `vp_village` (village, …→village 913) in `database/seed_demo.php` (user insert is now idempotent via `ON DUPLICATE KEY UPDATE` + re-select id). `seed_portal_access.php` grants them the MIS portal. All demo users have `Demo@123`.
- **E2E:** new `roles` suite (`node tests/e2e/run.js roles`) logs in as each of the five role users, asserts the redirect to their `home_*.php`, the unit banner text, no PHP warnings, and that visiting a wrong-role page bounces back to the user's own home. Creds for `block`/`panchayat`/`village` added to `tests/e2e/lib.js`.
- Gotcha: the E2E role loop must `goto('mis/logout.php')` before each login (login.php redirects an already-authenticated session away, so a stale session breaks the next case).

### Detailed, filterable records report + KPIs (this milestone)
- **`mis/detail_report.php` (NEW)** — scoped records report that pivots a form's answers into columns and renders KPI cards. Defaults to `GOVT_BUILDING_SURVEY` (form 40) when no `form_id` is chosen; guarded by `reports.view` (page) + `reports.export` (CSV).
- **`ReportService` (extended):** `DETAIL_COLUMNS` (curated column key→label map), `detailColumns(int $formId)` (only columns present in the form's published version; `district`/`block` appear only when the form has the `location` cascade), `detailWhere(array $filters)` (shared WHERE for report + KPIs: scope, form, status, date range, surveyor, keyword, district/block via location JSON, then **every `DETAIL_FILTERS` column**), `detailReport()`, `detailKpis()`, `detailFieldDistinct()` / `detailLocationDistinct()` (scoped dropdown values), `detailSurveyors()`, `toCsv()`.
- **All-column filtering:** `DETAIL_FILTERS` maps each extra column to a filter type — `exact` (department, dept_owner, building_category, building_subcategory, office_type, ownership_type, occupancy_status), `text` (building_name, building_code, contact_number), `range` (construction_year, num_floors, built_up_area, num_rooms). `detailWhere()` builds an `EXISTS` subquery per active filter against `survey_answers` (`COALESCE(value_text, value_number)` equality/LIKE for exact/text; numeric `CAST(...)` min/max with a `REGEXP '^[0-9]+([.][0-9]+)?$'` guard for range). District/block filter on the location JSON (`value_json → district_name/block_name`).
- **KPIs are "respective to that"** — they share the same `detailWhere()`, so every KPI card reflects the active filters. Cards: Total, Submitted, Verified, Approved/Published, Rejected, Total Built-up + avg, Total Rooms, **Departments, Categories**, districts covered, Latest Submission.
- **Gotchas:** `PDO::ATTR_EMULATE_PREPARES => false` → named params CANNOT repeat in one query, so generated params use per-column names (`:department_k`/`:department_v`, `:built_up_area_min`, etc.). Bootstrap `text-uppercase` on KPI labels means `innerText` returns `"TOTAL BUILDINGS"` — E2E must compare lower-cased text. `http_build_query` export URL must exclude the `viewer` User object.
- `common/views/layout.php` — sidebar "Detailed Report" link (gated by `reports.view`, active page `detail_report`).

### Server-side data-scope visibility (this milestone)
- **Requirement:** admin can view submitted data; portal users see only their own + sub-users' records/reports/GIS; Masters tab lists all master types; form-40 images/files persist when saved.
- `RecordService::scopeUserIds(User $viewer)` (STATIC): state_admin → `[]` (means "all"); surveyor → own id; hierarchy roles (district/block/panchayat/village) → all active users inside the viewer's lowest non-null scope column (`village_id` → `panchayat_id` → `block_id` → `district_id` → `department_id`) **plus self**. `RecordService::canView(User $viewer, array $record)` (STATIC) uses it.
- `RecordService::listRecords(..., ?User $viewer)` injects `r.user_id IN (...)`; `RecordService::find(int $recordId)` returns record + labelled answers (`field_label`, `field_type`), images, gps_logs, workflow history (answers fetched so `field_key` is present for all field types incl. `photo`).
- `RecordService::upsert()` now sets `submitted_by` = submitting user (insert) and `COALESCE(submitted_by, :sid)` on update (preserves original submitter on re-sync). Existing rows backfilled.
- **`mis/records.php` (NEW)** — record-detail page: human-readable answers table (master/cascade/GPS JSON rendering), attached files/images (web URLs), GPS logs, workflow history, scope guard (403 for non-allowed), Back to Monitoring.
- `mis/monitoring.php` — View button → `records.php?id=`; transitions + list guarded by `canView`; form dropdown filtered by `canAccessForm`; surveyor name column.
- **`mis/masters/index.php` rewritten** — "Masters" tab now lists ALL master groups (link into group → manage items), inline add-item, create-group modal, delete group/item (`masters.manage`), plus the location hierarchy column and CSV import.
- `ReportService` (all methods) + `mis/reports.php` + CSV export + `gis/gis_data.php` + `gis/index.php` + `api/Controllers/GisController.php` accept `?User $viewer` and scope-filter via `scopeUserIds()`.
- `api/Controllers/RecordController.php` — `index()` scoped; `transition()`/`show()`/`photos()` enforce `canView`. **`photos()` links uploads to answers**: optional `field_key` or `answer_id` param → sets `survey_images.answer_id` and writes `value_json = {image_id, file_path, original_name, category}` back to that `survey_answers` row so photo fields carry a resolvable path (verified end-to-end: file on disk, web URL 200, MIS detail renders image).
- **Permissions:** `database/migrations/003_data_view_permissions.sql` grants `monitoring.view`/`reports.view`/`reports.export`/`gis.view` to district/block/panchayat/village and `monitoring.view`/`reports.view`/`gis.view` to surveyor. Applied to live DB; also mirrored into `database/seed.sql` for fresh installs.
- Gotchas: `scopeUserIds()`/`canView()` are STATIC — new callers use `RecordService::` (calling via instance still works in PHP). `reset.php` now also purges `SMOKE_%` users (smoke sec. 12 creates them). Demo `dh_surveyor` has role code `district` despite the name.

### Government Building Survey (seed form)
- `database/seed_govt_building.php` builds `GOVT_BUILDING_SURVEY` from a PHP array: **17 sections, 132 fields**, includes master fields (`DEPARTMENT`, `BUILDING_SUBCATEGORY`) and a `location_cascade` field. Published as v1. Source of truth (field-by-field spec) is `Government_Building_Survey_Form.md` (untracked — do NOT commit).

### Mobile REST API additions (this milestone)
- `POST /v1/auth/*`, `GET /v1/forms` (access-filtered), `GET /v1/forms/{id}` (403 if no access), `POST /v1/records` (checks `canAccessForm`), `GET /v1/records` (scoped list), `GET /v1/records/{identifier}` (by id or uuid, with answers + images), `POST /v1/records/{id}/photos` (multipart `files[]` → `survey_images` + `uploads/survey/<recordId>/`), `POST /v1/devices` / `DELETE /v1/devices/{device_id}` (register + FCM token), `GET /v1/sync/status` (pending queue count by status), `/v1/location/*`, `/v1/master/*`, `/v1/notifications/*`, `/v1/gis/*`, `/v1/replication/*`, `/v1/health`.
- `RecordController` uses `api/user_data.php`-independent `identifier` lookup (id or uuid).

## Case file (form 40, browser-based)

- `tests/case_form40.js` — standalone headed-Chrome case (does NOT touch the E2E suite). Logs into MIS as `admin`, opens the form-40 builder preview (`mis/builder/preview.php?id=40`) so you can SEE the rendered form, then pushes a full form-40 record (127 answers: master, location_cascade, dropdowns, radios, multi_select, numbers, GPS) + 4 images (canvas-generated + a screenshot of the preview) through the real REST API (`/v1/records`, `/v1/records/{id}/photos` with `files[]` + `field_key` + `category`), then navigates Monitoring (form 40) → Record Detail to show answers + rendered images. KEEPS the record by default; `node tests/case_form40.js --cleanup [id]` deletes it + its files.
- Gotchas baked into the case: photo/signature fields must be submitted as (empty) answers first or `/photos` can't resolve `answer_id`; PHP only builds `$_FILES['files']` arrays when the multipart field is named `files[]`; the monitoring table shows record `#id`, not answer values.

## E2E Harness

- `tests/e2e/run.js` — headed Chrome (Puppeteer), resets DB at start + end. Runs `mis`, `roles`, and/or `admin` suites (`node tests/e2e/run.js [all|mis|roles|admin]`). Current total: 209 checks, all passing.
- Helpers in `tests/e2e/lib.js` (`BASE`, `CREDS`, `step/check/ok`, `clickText`, `type`, `waitForText`, `hasText`, `assertNoPhpWarnings`, `wirePage`, `summary`); dialogs auto-accepted.
- `type()` is self-verifying: triple-clicks, types with `delay:10`, reads back the field value and retries up to 3× (headed-Chrome truncates text typed during modal animation/network load).
- Form submits in tests go through `page.evaluate(form.submit())` (bypasses `onsubmit` confirm dialogs).
- Puppeteer does NOT support `:has-text()`; use `page.evaluate` + `Array.from(...).find`.
- E2E MIS suite now covers: create draft → publish → edit published (clone) → Save & Sync → verify pending badge cleared + sync button gone + broadcast flash, **create a user via modal (portal + form access), edit-user access persistence, admin Roles & Access page** (Manage modal shows portals/forms, submits, flash), **Gov't Building form 40** (editor loads 17 sections / 132 fields, dropdown options render, master groups persist, cascade shows 4 levels; preview renders dropdown/master selects; location cascade chains district→block→panchayat→village), **conditional logic** (save a condition on `e2e_location` referencing `e2e_dropdown`, condition editor row persists after reload, draft preview hides `e2e_location` until trigger `option_a` selected, shows on match, hides again on clear), **Masters tab** (lists all groups `DEPARTMENT`/`BUILDING_SUBCATEGORY`, "New Master Group" button, districts), **Monitoring data view** (surveyor name shown, view link present, verify button), **View submitted record detail** (opens `records.php?id=`, asserts answers incl. Landowner Name "Ravi Kumar", submitter/status, Back to Monitoring), and **Detailed Report** (KPI cards render case-insensitively, column filter panel + exact/range/text controls present, no PHP warnings; a building_category exact filter applies and stays selected on submit). **Role Dashboard suite** (`roles`): each of district/block/panchayat/village/surveyor logs in and lands on its `home_*.php`, unit banner shown, no PHP warnings, and a village user visiting `home_district.php` is bounced back to `home_village.php`. **Conditional store via API** (runs last in `mis` so its records can't shift the Monitoring list): `/v1/forms/{code}` exposes the `e2e_location` condition with resolved `target_field_key`; a record posted with the trigger unset drops the hidden `e2e_location` answer (201), trigger set but condition-required value missing returns 422 `e2e_location required`, and trigger set with the value stores it (201). **Sync queue via API** (same block): a device is registered via `/v1/devices`, and after the two 201 record stores `/v1/sync/status` reports `pending > 0`.
- `tests/smoke.php` covers `draftForEditing` clone/resume, `versionInfo` pending states, sync publish bump, live definition containing the synced field, broadcast recipient count, **conditional-logic round-trip** (condition saved by `target_field_key` resolves on load; a **forward-referenced** condition — targeting a field defined later — persists with a valid FK; values round-trip intact), **GOVT_BUILDING_SURVEY integrity (17 sections / 132 fields / master groups), portal + form-access helpers, state-admin implicit access, scope enforcement, block-admin role-assignment limits, assignableForms scoping**, **data-scope visibility** (`scopeUserIds` per role incl. surveyor = own id and block admin = block users + self, `canView` foreign-district block denied, state admin sees all, `submitted_by` set on upsert, labelled answers + submitter name in `find()`, `find()` null on missing, images surface with web paths, reports scoped admin=2 / block=0), **detailed-report all-column filters** (guarded against form-40 fixture: district filter narrows rows, `building_category` exact filter matches only, `built_up_area` min/max range applied, KPIs respond to a district filter, `departments`/`categories` KPIs reported), and **role dashboards** (`roleOf`/`homeUrl` for all 5 roles, district unit=Ranchi + sub-units + user/surveyor counts, block sub-units panchayat+village, panchayat/village unit resolution, surveyor stats scoped to own records = own-record count, surveyor has no user aggregation), **conditional-logic record store** (hidden-field answer dropped, condition-required validated on submit → 422, drafts skip validation but still drop hidden answers), **condition round-trip** (conditions keep a resolvable `target_field_key` after edit + re-publish, mandatory survives, hidden answers still dropped against the round-tripped version), and **mobile sync queue** (`enqueueSync` inserts a pending `upsert` item with the record metadata in `payload_json`, re-enqueue replaces the pending item instead of duplicating, `/v1/sync/status` pending reflects it, unknown `device_id` falls back to active devices, deleting the device cascades its queue rows). (Note: notification "send/deliver" check asserts id presence in `forUser()` list, not index 0 — same-second ordering is nondeterministic.)

## Milestones (git history)

- (this session, will commit) **mobile2 Capacitor app + signed release APK:** mobile rebuilt as `mobile2/` (Capacitor 8 + TS + Bootstrap + Alpine + SQLite, `com.jsac_bcd_survey.app`); web+Vite + `cap sync` pipeline, JDK 21 Gradle builds; LDPlayer 9 is the emulator (`emulator-5554`, `adb reverse tcp:8080 tcp:81`); `assembleRelease` produces a **signed** `app-release.apk` (~49.9 MB, `CN=BCD Survey`) via gitignored keystore; `keystore.properties` + `.jks` added to `mobile2/.gitignore`; smoke range-filter assertion fixed (data-dependent). Backend smoke all green. Full on-device runtime verification on LDPlayer still pending (Next Steps).
- (this session, will commit) **fresh machine setup + mobile runtime verification (backend DONE, mobile BLOCKED on network):** backend fully set up & verified (see Goal) with 3 code fixes: `database/migrate.php` `splitStatements()` now skips `--` line and `/* */` block comments (semicolons inside `--` comments in `schema.sql:176`/`seed.sql:61` broke statement splitting), `database/migrations/003_data_view_permissions.sql` → `INSERT IGNORE` (it re-inserts grants seed.sql already applied, duplicate-key on fresh installs), `tests/smoke.php` resolves demo users by username instead of hardcoded ids (`User::find(3/4/5)` were old-DB ids); plus a new root `index.php` (redirect → `mis/login.php`) because `/bcd-app/` 403'd (no index file, `Options -Indexes`), and E2E `run.js` hardcoded form 40 → `govt = {form:2, dept:2, sub:3}` + `PHP_BIN` env support. Smoke: all green. E2E last full run: 170 checks — govt-building + role-dashboard suites fixed; one flaky failure in `admin` suite at `waitForSelector('#ac_portal_mis')` (a standalone probe confirmed the modal DOES render visible in headed Chrome — timing flake, rerun may pass; second run also showed a `form 40`-era govt timeout before the fix). Mobile: `npm install --legacy-peer-deps` (see gotcha), `npx tsc --noEmit` + `npm run lint` + `npm test` all clean, **`gradlew assembleDebug` FAILED after ~14 min on `repo.maven.apache.org` hostname-resolution errors** (network issue, NOT code) → `app-debug.apk` does NOT exist yet. AVD on THIS machine is `Pixel_4a` (x86_64, API 36, PlayStore disabled) — `Medium_Phone_API_36` does not exist here; SDK has platforms 33–37.0 + build-tools 34.0.0–37.0.0. Next: retry `gradlew assembleDebug` when the network is stable, then finish the runtime-verification checklist below.
- (this session, in progress, NOT yet committed) **mobile runtime verification started:** backend `/v1/health` = 200; AVD `Medium_Phone_API_36` boots headless; `app-debug.apk` installs on it; the app launches (`com.bcdmobile/.MainActivity` in foreground) and connects to Metro (`isMetroRunning = true`, `loadJSBundleFromMetro()`). **NOT yet verified:** the login screen / login-as-admin against `http://10.0.2.2:81/bcd-app/api/v1`, the Home forms list (GOVT_BUILDING_SURVEY), dynamic form rendering, and record submit → `/v1/sync/status`. Was mid-boot of a fresh emulator instance when this session ended. Key gotchas captured above (adb kill-server kills the emulator's adb link; Metro must be launched detached; debug APK requires Metro via `adb reverse`).
- `40ca703` — **mobile app upgraded to RN 0.87.0-rc.3 for AGP 9 + API 37**: RN 0.86's Gradle plugin is incompatible with AGP 9 (`finalizeDsl` rejected after AGP 9 finalizes a library DSL; KGP <2.2 can't consume the AGP 9 DSL), so the app was re-inited fresh at 0.87.0-rc.3 (first line with the AGP 9 fix, facebook/react-native#57038) and `src/`, `App.tsx`, `README.md` restored. New toolchain ships in the template: AGP 9.2.1 (RN Gradle plugin pins it), Gradle wrapper 9.4.1, `compileSdk 37` / buildTools 37.0.0 / targetSdk 36 / minSdk 24, kotlin 2.2.0, `edgeToEdgeEnabled=true`, and `android.builtInKotlin=false` + `android.newDsl=false` in `gradle.properties`. Re-applied on the fresh template: location permissions in AndroidManifest; deps async-storage 2.1.2 / geolocation / device-info / keychain (safe-area-context ships in template; dropped unused react-native-screens); `jest.setup.js` mocking native modules (device-info, keychain, async-storage, geolocation) wired via `jest.config.js` so `App.test.tsx` renders. Machine trimmed to only the API 37 platform + 37.0.0 build-tools; Gradle home keeps only the 9.4.1 distribution. Verified: tsc/lint/jest clean, production bundle builds, `./gradlew assembleDebug` succeeds (216 tasks) → `app-debug.apk`.
- `54bad15` — **mobile app: core offline-first survey loop (React Native, Android)** (`mobile/`): RN 0.86 scaffold; `src/api/` (`client.ts` single-flight refresh, `endpoints.ts`, `cache.ts` AsyncStorage offline store + pending queue + device id, `session.ts` Keychain), `src/context/` (`AuthContext` login/logout/bootstrap + best-effort device registration; `DataContext` offline load/refresh/download/queue + auto-upload on foreground), `src/screens/` (`Login`, `Home` forms list, `FormFill` dynamic form with client-side mirrors of `ConditionEvaluator` + validators, `Sync` pending + `/v1/sync/status`), `src/components/fields/` (text/number/date, radio/checkbox/multi-select, dropdown, master, location_cascade, GPS; photo/signature/file/barcode/qr show a "not available offline yet" note), `src/utils/` (`conditions.ts`, `validators.ts`). Android: location permissions + per-build-type `usesCleartextTraffic` placeholders (debug only); dev API base `http://10.0.2.2:81/bcd-app/api/v1`. Typecheck + eslint clean; production JS bundle builds. Full Phase 4 gaps (camera/signature/file upload, SQLite, background worker, notification-triggered form download) remain in Next Steps.
- `c1a4eec` — **mobile sync queue populated on record store (Phase 4)**: `RecordService::enqueueSync()` (pending `upsert` item keyed to the submitted `device_id` or falling back to the user's active devices; re-sync replaces the pending item; payload carries record metadata), wired best-effort into `RecordController::store()` (queue failure must not reject the persisted record); `/v1/sync/status` now reports real pending work; `docs/API.md` documents `device_id?` + `/v1/sync/status`; smoke section 18 (enqueue, payload, no-duplicate re-sync, pending count, device fallback, cascade cleanup) + E2E +2 checks (device registered, sync pending>0) → 209 all green.
- `3729f81` — **server-side conditional-logic evaluation on record store (Phase 3)**: `ConditionEvaluator` (`evaluate`/`missingRequired`, mirrors preview engine), `App\Exceptions\ValidationException`, `RecordService::upsert()` drops hidden-field answers + enforces mandatory/condition-required (drafts skip required checks), `RecordController::store()` → 422; fixed `saveStructure()` condition round-trip (resolve target by field_key against fresh ids) + `is_mandatory` preservation; smoke section 17/17b; E2E +4 checks (API conditional store: definition exposes conditions, hidden answer dropped, condition-required 422, visible answer stored) → 207 all green.
- (this session) `34d022b` — **data-scope visibility**: `RecordService::scopeUserIds()/canView()` (static), `submitted_by` backfill, `mis/records.php` detail page, full Masters tab (`mis/masters/index.php` rewritten), ReportService/GIS scoped, photo→answer linking in `RecordController::photos()`, `database/migrations/003_data_view_permissions.sql` + seed.sql grants; E2E 165→173, all green; smoke green.
- (this session) `ee3b881` — **browser-based case file** `tests/case_form40.js` (push form-40 record with images via REST; keeps record by default; `--cleanup [id]` removes it) + `.gitignore` entries for its runtime artifacts.
- `c17ff4f` — **role-specific dashboards + panchayat/village demo users**: `RoleDashboardService` (`roleOf`/`stats` with unit/sub-units/users/records/forms), `User::homeUrl()` routing (login + sidebar + `dashboard.php` guard), 5 thin `mis/home_*.php` pages + shared `common/views/partials/role_dashboard.php`, idempotent demo-user seed (adds `pm_panchayat`, `vp_village`), portal seed updated; smoke section 16; E2E new `roles` suite (+21 checks → 203), all green.
- (this session) `67ea762` — **detailed, filterable records report + all-column filters + scope-aware KPIs**: new `mis/detail_report.php`, `ReportService` `DETAIL_COLUMNS`/`DETAIL_FILTERS`/`detailWhere` (generic EXISTS per column; district/block via location JSON; numeric range with REGEXP guard)/`detailReport`/`detailKpis` (adds Departments + Categories)/`detailFieldDistinct`/`detailLocationDistinct`/`detailSurveyors`; sidebar "Detailed Report" link; smoke section 15b; E2E +9 checks → 182, all green (KPI labels are `text-uppercase`, so E2E compares lower-cased text); CSV export URL excludes `viewer`.
- (this session) `d3ce96c` — **conditional logic engine (IF/THEN show/hide/required)**: builder condition editor, two-pass save with `target_field_key`/forward refs, preview engine + E2E/smoke tests (165 checks); `abeecd0` — mobile API records/devices/sync endpoints; `9641666` — Government Building Survey form + RBAC portal/form access; `4efc019` — fix location cascade chain in builder preview + Gov't Building E2E tests.
- `1c55442` — edit published form + sync to all users (builder sync flow + E2E).
- `06b5a1c` — location_cascade field type + `/v1/location/*` API + scope-aware preview + `Request::header()` fix. (previous)
- `7641300` — master-data field type (admin CRUD, builder linkage, id+name storage) + draft-form preview.
- `d088f8b` — survey builder option save fix + user-modal race fix + `plain_password` dev column.
- `e1b958c` — E2E suite (headed Chrome) + bugs it surfaced (.htaccess API rewrite, absolute sidebar URLs, `../../api/` fetch fix, audit logging for auth + transitions).
- `9279818` — gitignore fix (untracked nested `tests/e2e/node_modules`).
- `f9152c5` — admin panel + Jharkhand seed data.
- `400b6c0` — notifications, replication queue, docs, smoke tests.

## Next Steps / Ideas (not yet done)

- **Mobile2 runtime verification on LDPlayer (DONE):** APK(s) build + sign (done). Remaining verified: launch LDPlayer (`ldconsole launch --index 0`), `adb install -r` debug APK, `adb reverse tcp:8080 tcp:81`, `am start`, then confirm (a) login screen → login `admin`/`Admin@12345` against `http://127.0.0.1:8080/bcd-app/api/v1` (via reverse tunnel) ✓, (b) Home lists GOVT_BUILDING_SURVEY ✓, (c) form renders dynamic fields — master (Department with master items) ✓, location cascade (District→Block→Panchayat→Village, all 24 districts populated) ✓, GPS field present ✓, photos field present ✓, conditional logic fields exist in code but no published form exercises them currently, (d) submit a record → `/v1/sync/status` reports `pending: 1` ✓. Full on-device runtime verification complete. See `mobile2/RUN_COMMANDS.md` for details.
- **Release config:** `src/js/config/index.ts` still points at `http://127.0.0.1:8080/bcd-app/api/v1` (dev tunnel) — release builds must switch to an HTTPS endpoint (cleartext/mixed content are dev-only).
- The old RN app (`mobile/`, RN 0.87.0-rc.3) is superseded by `mobile2/` — decide whether to remove it (kept in repo for now).
- **Mobile app core offline-first loop IS in the repo now** (`mobile/`, committed this session): login → download forms/masters/locations → fill offline (master / location_cascade / GPS / dropdown / conditional show+required) → queue locally → auto-upload on foreground / manual Upload Now → view `/v1/sync/status`. Still missing for full Phase 4: camera/signature/file-upload fields, SQLite storage (AsyncStorage today), a background worker for periodic sync, and the "new form version" push-notification → download flow (the server broadcasts via `NotificationService`; the app must implement the download-on-notify flow against `/v1/forms`).
- `SurveyService::publishedForms()` still returns full definitions for ALL published forms at the service level — reports/monitoring/GIS now filter by `canAccessForm` + data scope, but `mis/builder/index.php` may still list all forms to scoped users (out of scope unless asked).
- `user_form_access.can_fill` / `can_view` columns exist but are not yet distinguished on the mobile side.
- Conditional logic (show/hide/required) is now persisted + validated server-side on record store (`ConditionEvaluator` + 422 via `RecordController::store()`) and exposed to mobile via `/v1/forms/{id}` (done this session).
- Mobile sync queue (`sync_queue` table) is now populated by `RecordController::store()` via `RecordService::enqueueSync()` (action `upsert`, best-effort, targeted at the submitted `device_id` or falling back to the user's active devices; re-syncs replace the prior pending item). `/v1/sync/status` reflects real pending work.
- Possible future work: `master` + `location_cascade` answer rendering in MIS reports/monitoring tables; cascade auto-population on edit of an existing record; validation of master/cascade answers on the API; field-level permissions; schedule/expiry on published versions.

## Relevant Files

- **Schema/Seed:** `database/schema.sql`, `database/seed.sql` (data-view grants for hierarchy roles + surveyor), `database/seed_demo.php` (idempotent demo users incl. `pm_panchayat`/`vp_village`), `database/seed_jharkhand.php`, `database/seed_govt_building.php` (GOVT_BUILDING_SURVEY), `database/seed_portal_access.php`, `database/migrations/002_portal_form_access.sql`, `database/migrations/003_data_view_permissions.sql`
- **Core services:** `common/src/Services/SurveyService.php`, `common/src/Services/RecordService.php` (static `scopeUserIds`/`canView`, `find()`, `submitted_by`, condition-evaluated store, `enqueueSync` for the mobile sync queue), `common/src/Services/ConditionEvaluator.php` (`evaluate`/`missingRequired`), `common/src/Services/ReportService.php` (viewer-scoped), `common/src/Services/RoleDashboardService.php` (`roleOf`/`stats` for role landing pages), `common/src/Services/UserService.php` (scope + access), `common/src/Exceptions/ValidationException.php`
- **HTTP/Auth:** `common/src/Http/Request.php` (header fix), `common/src/Http/Router.php`, `common/src/Http/Response.php`, `common/src/Auth/SessionAuth.php`, `common/src/Auth/ApiAuth.php`
- **API:** `api/index.php`, `api/dropdowns.php`, `api/user_data.php` (portals + form_ids + scope), `api/Controllers/LocationController.php`, `api/Controllers/MasterController.php`, `api/Controllers/FormController.php` (access-filtered), `api/Controllers/RecordController.php` (scoped index/show/transition, photo→answer linking, conditional-validation 422 + sync-queue enqueue on store, `/v1/sync/status`), `api/Controllers/DeviceController.php`, `api/Controllers/GisController.php` (scoped)
- **Builder UI:** `mis/builder/edit.php` (condition editor), `mis/builder/preview.php` (conditional engine + cascade), `mis/builder/index.php`, `mis/builder/sync.php` (publish + notify), `mis/builder/publish.php`
- **MIS pages:** `mis/monitoring.php` (scoped + View button), `mis/records.php` (record detail — answers/images/GPS/workflow), `mis/masters/index.php` (all masters + inline items), `mis/reports.php` (scoped + CSV), `mis/detail_report.php` (detailed report: all-column filters + scope-aware KPIs + CSV), `mis/home_district.php` / `home_block.php` / `home_panchayat.php` / `home_village.php` / `home_surveyor.php` (role landing pages), `mis/users/index.php` (portal + form access modal), `mis/users/save.php` (actor-scoped), `mis/login.php` (portal gate + role-based redirect), `mis/logout.php`, `mis/dashboard.php` (state-admin dashboard; redirects scoped users to their role home)
- **GIS pages:** `gis/gis_data.php`, `gis/index.php` (scoped points + forms)
- **Admin panel:** `admin/access.php` (Roles & Access), `admin/masters.php`, `admin/dashboard.php`, `admin/settings.php`, `admin/notifications.php`, `admin/audit.php`, `admin/replication.php`, `admin/health.php`
- **Layouts:** `common/views/layout.php` (MIS), `common/views/admin_layout.php` (admin), `common/views/partials/role_dashboard.php` (shared role dashboard content)
- **Tests:** `tests/smoke.php`, `tests/e2e/run.js`, `tests/e2e/lib.js`, `tests/e2e/reset.php`, `tests/case_form40.js` (browser case: push form-40 record + images)
- **Config (gitignored):** `config/.env` (`APP_URL=http://localhost:81/bcd-app`)
- **Mobile app:** `mobile/` (React Native **0.87.0-rc.3**, Android; AGP 9.2.1 / Gradle 9.4.1 / API 37) — `App.tsx` (providers + modal navigation), `src/config/index.ts` (API base URL), `src/api/` (`client.ts`, `endpoints.ts`, `cache.ts`, `session.ts`), `src/context/` (`AuthContext.tsx`, `DataContext.tsx`), `src/screens/` (`LoginScreen`, `HomeScreen`, `FormFillScreen`, `SyncScreen`), `src/components/fields/` (`FieldShell`, `TextFieldInput`, `OptionInput`, `DropdownInput`, `PickerModal`, `MasterInput`, `CascadeInput`, `GpsInput`, `FieldRenderer`), `src/utils/` (`conditions.ts`, `validators.ts`), `src/types/api.ts`; Android config in `android/` (`app/build.gradle` cleartext via RN plugin, `app/src/main/AndroidManifest.xml` location permissions), `jest.setup.js` (native-module mocks); `README.md` run instructions
- **Root:** `.htaccess`, `.gitignore`, `ROADMAP.md`, `readme.md`, `docs/API.md`, `docs/INSTALL.md`, `MEMORY.md` (this file)
